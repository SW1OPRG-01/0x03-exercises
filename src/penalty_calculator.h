#include <vector>

using namespace std;

double calculate_violation_percent(float, int);
int calculate_fine(vector<int>, float, int);
int calculate_high_speed_fine(int);
bool speed_limit_high(int);
bool speed_limit_normal(int);
vector<int> rate(int, int);